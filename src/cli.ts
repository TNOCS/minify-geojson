export interface ICommandOptions {
    keys: boolean,
    includeKeyMap: boolean,
    from: string,
    topo: boolean,
    verbose: boolean,
    coordinates: number,
    src: string[],
    whitelist: string,
    blacklist: string
}
